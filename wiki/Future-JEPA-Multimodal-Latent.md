# Future -> JEPA / Multimodal Latent

Status -> idea parking only.
Current runtime -> do not widen for this yet.

## Direction

text Coconut -> latent reasoning core.
future multimodal -> separate owner only after concrete PRD.
JEPA-style objective -> research branch, not runtime dependency.

## Guardrail

Do not add abstractions now.
Benchmark need -> PRD -> tracer slice -> package owner.
